global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'playground-node'
    static_configs:
      - targets: ['{{ playground_ip }}:{{ node_exporter_port }}']
        labels:
          instance: 'playground'
