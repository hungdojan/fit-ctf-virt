{%- if network.dhcp_server -%}
config:
  ipv4.address: {{ network.dhcp_server }}
  ipv4.nat: true
  ipv6.address: none
{%- else -%}
config: {}
{%- endif %}
name: {{ network.name }}
type: {{ network.type }}
description: {{ network. description }}
